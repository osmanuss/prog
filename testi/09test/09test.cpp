#include <iostream>
#include <typeinfo>

using std::cout;
using std::cin;

int main()
{
	int x = 5;
	
	//1
	//cout << (x && x * x);

	//2
	//cout << true;

	//3
	//cout << (x || x * x);

	//4
	//auto b = (x and 1);
	//cout << typeid(b).name();
}